Generate 200 unique physical parts across categories (fasteners, bearings, seals, electrical). For each part, create 2-4 supplier records with realistic naming variations — different abbreviations, different ordering, different levels of detail, some with specs missing. Also include 30 parts that only exist in one source (no match should be found). Output as CSV with columns: source, part_number, part_description, material, dimensions, category. Also output a separate truth table mapping which part_numbers are the same physical part.